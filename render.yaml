services:
  - type: web
    name: voicesafe-ai
    env: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y ffmpeg
    startCommand: python app.py